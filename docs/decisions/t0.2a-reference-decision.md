# T0.2a reference decision

```yaml
schema_version: geoembeddings-decision/1.0
task_id: T0.2a
status: pending
artifact_index: docs/artifacts/t0.2-reference500.json
selected_action: finish_evaluator_gate
aggregate_winner: null
```

## Audit state

**T0.2a remains pending.** The machine-readable reference index reports that no
immutable local artifact location or durable external-storage identifier is
available. Consequently, source lineage and matched baseline/learned inputs
cannot be validated. See `docs/artifacts/t0.2-reference500.json`.

## Per-axis decision

| Axis | Evidence required | Audit result | Decision |
|---|---|---|---|
| Persistent probes | Matched persistent-trait and category-preference probe reports, including label/user coverage | Required report identifiers are missing | Pending; do not infer persistent preference recovery |
| Incremental information | Matched geography/event-volume controls and incremental probe deltas | Required report identifiers are missing | Pending; do not infer information beyond opportunity proxies |
| Collapse and geometry | Same-user stability together with separation, temporal retrieval, centered effective rank, and task information | Required report identifiers are missing | Pending; stability alone cannot rule out collapse |
| Episode response | Matched dense exports and episode reports with common episode/user/bin coverage | Required export and report identifiers are missing | Pending; do not infer context adaptation or disentanglement |
| Event-removal robustness | Matched removal masks/rates, realized perturbations, users, and downstream reports | Required robustness report identifiers are missing | Pending; do not infer robustness |
| Next-event performance and coverage | Matched target metrics, known-label coverage, users, and cutoffs | Required evaluation report identifiers are missing | Pending; do not infer predictive superiority |

No aggregate score or aggregate winner is declared. Every axis remains separate,
and absence of evidence is not treated as a tie or a zero delta.

## Selected action

**Selected action: finish the evaluator gate.** This is the one and only selected
action. `repair/ablate` and `factorize` are not selected. The implementation of
T1.4 supplies additional evaluator surfaces, but it does not repair the absent
T0.2 archive and does not make this post-reference decision complete.

## Unblocking condition

T0.2 and T0.2a may be checked only after the index names durable identifiers for
every required artifact and records a successful comparison of source hashes,
cutoffs, categorical field order, users, and preparation contract. The per-axis
table must then be populated from those reports without combining the axes.
