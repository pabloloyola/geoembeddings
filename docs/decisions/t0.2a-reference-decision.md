# T0.2a post-reference decision

**Decision:** finish the evaluator gate (T1.4). This is the only selected next
action; repair/ablate and factorization remain gated.

The per-axis review did not produce an aggregate winner. Persistent/preference
probes, incremental information, geometry/collapse diagnostics, episode
response, event-removal robustness, and next-event performance remain separate
axes. In particular, the existing single-vector observational results cannot
establish causal invariance or disentanglement. The decision therefore selects
the missing R6/R7 evaluator surfaces before any model change.

The generated 500-user artifacts are not stored in Git. Their external archive
must remain associated with its source hashes, cutoffs, users, and preparation
contract; this decision record does not reproduce or reinterpret those metrics.
