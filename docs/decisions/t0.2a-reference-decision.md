# T0.2a reference decision

```yaml
schema_version: geoembeddings-decision/1.0
task_id: T0.2a
status: complete
artifact_index: docs/artifacts/t0.2-reference500.json
selected_action: finish_evaluator_gate
aggregate_winner: null
```

## Audit state

**T0.2a is complete as an evidence-absence decision.** T0.2 is closed as
evidence lost/unverifiable: the indexed local
roots and the recorded source commit are absent, and no durable external-storage
identifier is available. Consequently, source lineage and matched
baseline/learned inputs cannot be validated. Completion records that disposition
and selects the next action; it does not supply scientific evidence. See
`docs/artifacts/t0.2-reference500.json`.

## Per-axis decision

| Axis | Evidence required | Audit result | Decision |
|---|---|---|---|
| Persistent probes | Matched persistent-trait and category-preference probe reports, including label/user coverage | Indexed reports are unavailable and their bytes cannot be authenticated | Pending; do not infer persistent preference recovery |
| Incremental information | Matched geography/event-volume controls and incremental probe deltas | Indexed reports are unavailable and their bytes cannot be authenticated | Pending; do not infer information beyond opportunity proxies |
| Collapse and geometry | Same-user stability together with separation, temporal retrieval, centered effective rank, and task information | Indexed reports are unavailable and their bytes cannot be authenticated | Pending; stability alone cannot rule out collapse |
| Episode response | Matched dense exports and episode reports with common episode/user/bin coverage | Indexed exports and reports are unavailable and their bytes cannot be authenticated | Pending; do not infer context adaptation or disentanglement |
| Robustness | Matched GPS, timestamp, service-removal, and truncation masks, realized perturbations, users, and downstream reports | Indexed views and reports are unavailable and their bytes cannot be authenticated | Pending; do not infer robustness |
| Next-event performance and coverage | Matched target metrics, known-label coverage, users, and cutoffs | Indexed reports are unavailable and their bytes cannot be authenticated | Pending; do not infer predictive superiority |

No aggregate score or aggregate winner is declared. Every axis remains separate,
and absence of evidence is not treated as a tie or a zero delta.

## Selected action

**Selected action: finish the evaluator gate.** This is the one and only selected
action. `repair/ablate` and `factorize` are not selected. The implementation of
T1.4 supplies additional evaluator surfaces, but it does not repair the absent
T0.2 archive. The decision is complete as an absence audit, not as a scientific
comparison.

## Unblocking condition

The scientific axes may be populated only after the exact indexed bytes are
recovered, made durably retrievable, and successfully reauthenticated against
every hash. If recovery is impossible, a replacement reference must use a new
identity and evidence-index lineage. The per-axis table must then be populated
from readable reports without combining the axes. The completed checkbox denotes
this decision audit, not recovery or scientific success.
